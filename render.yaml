# Render: https://render.com/docs/blueprint-spec
# Set DATABASE_URL in the dashboard (Supabase pooler URI). Do not commit secrets.
# CINDY_LOGIN_SECRET is optional: if unset, the server stores a random JWT secret in Postgres once.
# Optional one-time: CINDY_SEED_DEMO_ACCOUNTS=true forces demo user password sync on every boot.
# Set CINDY_SEED_DEMO_ACCOUNTS=false to disable automatic demo sync entirely.
services:
  - type: web
    name: cindy-platform
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: CINDY_LOGIN_SECRET
        sync: false
      - key: CINDY_ALLOW_PUBLIC_ACCESS
        sync: false
      - key: CINDY_LEAD_EMAILS
        sync: false
      - key: CINDY_COORDINATOR_EMAILS
        sync: false
      - key: NODE_ENV
        value: production
      - key: KEEP_ALIVE_ENABLED
        value: "true"
      - key: KEEP_ALIVE_INTERVAL_MS
        value: "480000"
      - key: KEEP_ALIVE_URL
        value: "https://cindy-platform.onrender.com"
